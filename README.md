A terminal based 'worldle' game where you have to determine the random selected country by the distance between that and your guesses.

Create a virtual environment, activate and install the requirements
```
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python worldle.py
```
